---
display:
  ellipsis: …
  pum_max_len: 66

match:
  unifying_chars:
    - _
    - "-"
  timeout: 0.1
  transpose_band: 3

weights:
  prefix_matches: 3.0
  insertion_order: 2.0
  match_density: 2.0
  nearest_neighbour: 2.0
  consecutive_matches: 1.5
  num_matches: 1.0
  count_by_filetype: 1.0
  alphabetical: 0.5

clients:
  tree_sitter:
    short_name: "TS"
    weight: 1.5
  lsp:
    short_name: "LS"
    weight: 1.5
  buffers:
    short_name: "B"
    weight: 1.0
    prefix_len: 2
  paths:
    short_name: "F"
    weight: 2.0
  tmux:
    short_name: "TMX"
    weight: 1.0
    prefix_len: 2
    polling_interval: 1.0

